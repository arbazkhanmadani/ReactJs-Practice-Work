import { useNavigate } from "react-router-dom"

//USENAVIGATE =>
/*
This Hook is used for for the dynamic routing
Having fire the url and have to go a perticular destination then we can use it.
Such AS = having posting data to server and go to the dashboard etc siatuation.

*/

import { useNavigate } from "react-router-dom"

const navigate = ()=>{

    const useNavig = useNavigate()

    /* 
        fetch(){
            .....
            //In end
            useNavig('/dashboard')
        }
    */
}